// Kept as a compatibility export for integrations that imported the old
// settings component. Provider configuration now lives in its own drawer.
export { ProviderChannelsDrawer as SettingsDrawer } from "./ProviderChannelsDrawer";
